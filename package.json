{
  "private": true,
  "name": "resume",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "dependencies": {
    "@csstools/normalize.css": "^12.1.1",
    "@mdx-js/preact": "^3.0.1",
    "preact": "^10.15.0",
    "weui-icon": "^1.0.2"
  },
  "devDependencies": {
    "@mdx-js/loader": "^3.0.1",
    "@rspack/cli": "0.6.5",
    "@rspack/core": "0.6.5",
    "@types/mdx": "^2.0.13",
    "@types/webpack-env": "^1.15.2",
    "rspack-plugin-prefresh": "^0.6.5",
    "typescript": "~5.1.6"
  },
  "scripts": {
    "build": "rspack build --mode=production --node-env=production",
    "build:dev": "rspack build --mode=development",
    "build:prod": "rspack build --mode=production --node-env=production",
    "serve": "rspack serve",
    "dev": "rspack serve --mode=development"
  }
}
