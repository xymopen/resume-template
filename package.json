{
  "private": true,
  "name": "resume",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "dependencies": {
    "@csstools/normalize.css": "^12.1.1",
    "@mdx-js/preact": "2.3.0",
    "preact": "^10.24.1",
    "weui-icon": "^1.0.2"
  },
  "devDependencies": {
    "@mdx-js/loader": "2.3.0",
    "@rspack/cli": "1.0.5",
    "@rspack/core": "1.0.5",
    "@rspack/plugin-preact-refresh": "1.0.1",
    "@types/mdx": "ts5.5",
    "@types/webpack-env": "ts5.6",
    "typescript": "~5.5.2"
  },
  "scripts": {
    "build": "rspack build --mode=production --node-env=production",
    "build:dev": "rspack build --mode=development",
    "build:prod": "rspack build --mode=production --node-env=production",
    "serve": "rspack serve",
    "dev": "rspack serve --mode=development"
  }
}
